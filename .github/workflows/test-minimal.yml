name: Test Debug 7

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  first:
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.gen.outputs.value }}
    steps:
      - id: gen
        run: echo "value=hello" >> "$GITHUB_OUTPUT"
