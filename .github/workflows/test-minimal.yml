name: Test Debug 4

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - run: echo "first"

  second:
    needs: first
    runs-on: ubuntu-latest
    steps:
      - run: echo "second"
