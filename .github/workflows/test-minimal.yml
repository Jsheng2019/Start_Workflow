name: Test Debug 10

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: 20

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Node: ${{ env.NODE_VERSION }}"
