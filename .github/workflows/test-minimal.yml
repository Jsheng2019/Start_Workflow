name: Test Debug 8

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      test-input:
        description: 'Test input with expression ${{ github.repository }}'
        required: false
        type: string

permissions:
  contents: read

env:
  TEST_VAR: ${{ github.repository }}

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Repo: ${{ env.TEST_VAR }}"
